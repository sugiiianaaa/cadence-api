# Copy to .env on the VPS — never commit .env
ConnectionStrings__Default=Host=cadence-db;Database=cadence;Username=cadence_dev;Password=changeme
ApiKey=your-api-key-here
POSTGRES_DB=cadence
POSTGRES_USER=cadence_dev
POSTGRES_PASSWORD=changeme
